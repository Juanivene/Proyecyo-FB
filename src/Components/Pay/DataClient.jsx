import Grid from "../Grid/grid";
import FormDataClient from "./FormDataClient";

const DataClient = () => {
  return (
    <section>
      <FormDataClient />
    </section>
  );
};

export default DataClient;
